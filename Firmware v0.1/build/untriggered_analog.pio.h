// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ------------------ //
// untriggered_analog //
// ------------------ //

#define untriggered_analog_wrap_target 0
#define untriggered_analog_wrap 1

static const uint16_t untriggered_analog_program_instructions[] = {
            //     .wrap_target
    0x5008, //  0: in     pins, 8         side 0     
    0x1800, //  1: jmp    0               side 1     
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program untriggered_analog_program = {
    .instructions = untriggered_analog_program_instructions,
    .length = 2,
    .origin = -1,
};

static inline pio_sm_config untriggered_analog_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + untriggered_analog_wrap_target, offset + untriggered_analog_wrap);
    sm_config_set_sideset(&c, 2, true, false);
    return c;
}

static inline void untriggered_analog_program_init(PIO pio, uint sm, uint offset, uint pin_base, uint side_set, float clkdiv) {
    pio_sm_config c = untriggered_analog_program_get_default_config(offset);
    sm_config_set_sideset_pins(&c, side_set);
	sm_config_set_sideset(&c, 2, true, false);
	pio_gpio_init(pio, side_set);
    // Set the IN base pin to the provided `pin_base` parameter
    sm_config_set_in_pins(&c, pin_base);
    // Set the pin directions to input at the PIO
    pio_sm_set_consecutive_pindirs(pio, sm, pin_base, 8, false);
	pio_sm_set_consecutive_pindirs(pio, sm, side_set, 1, true);
    // Set clock divider
    sm_config_set_clkdiv(&c, clkdiv);
    // Shifting to left.
    sm_config_set_in_shift(
        &c,
        false, // Shift-to-right = false (i.e. shift to left)
        true,  // Autopush enabled
        8      // Autopush threshold = 8
    );
    // We only receive, so disable the TX FIFO to make the RX FIFO deeper.
    sm_config_set_fifo_join(&c, PIO_FIFO_JOIN_RX);
    // Load our configuration, and start the program from the beginning
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true); 
}

#endif

